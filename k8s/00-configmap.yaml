apiVersion: v1
kind: ConfigMap
metadata:
  name: helpdesk-config
  namespace: helpdesk
data:
  # Non-sensitive config — ใส่ที่นี่ได้ไม่กลัวถูกเห็น
  # Sensitive values (passwords, tokens) → ใช้ Secret แทน
  NODE_ENV: "production"
